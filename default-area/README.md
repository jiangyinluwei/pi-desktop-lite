# Default Workspace Area (Pi Desktop Lite)

This directory serves as the default isolated workspace for Pi Agent.

- **`AGENTS.md`**: Default agent runtime description and instructions for Pi Agent.
- All files, scripts, or project assets generated during conversational sessions are created here by default.
- This directory is automatically packaged into the application bundle resources upon build.
